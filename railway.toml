[build]
builder = "nixpacks"
installCommand = "npm install"
buildCommand = "npm run build"

[deploy]
startCommand = "NODE_OPTIONS='--dns-result-order=ipv4first' npm run start"
restartPolicyType = "on_failure"
